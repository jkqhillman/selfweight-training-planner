# 更新日志

## 未发布 · 2026-08-23

- 确认当前源码基准为提交 `3813349`（2026-08-14），最终生效视觉样式为蓝色、白底、直线分隔版；过期缓存中的绿色圆角界面不属于当前版本。
- Service Worker 版本更新为 `train-well-v50`，页面导航改为网络优先，网络超过 1.5 秒才回退到本地缓存，减少旧界面长期滞留。
- 更新安装包只预缓存 HTML、CSS、JavaScript、清单和应用图标，不再在每次更新时下载整套动作图片。
- 动作指导图片改为首次打开对应指导时按需缓存。
- `index.html` 中的 CSS 和 JavaScript 资源版本号同步为 `v50`。
- 修复英文界面中完成、开始、撤销按钮的“第 1 组”以及“左 / 右”组标签未翻译的问题。
- 保存提醒时间后立即重新检查，并清除当天旧的提醒标记；页面打开期间每 30 秒重新检查一次。
- 修复设置页静态标签带尾随空格时无法命中英文翻译的问题。
- 将底部导航“修复”的英文统一为 `Recovery`，避免误译为 `Reset`。
- 增强启动容错：损坏或过期的本地训练状态会自动回到有效训练计划，避免刷新后动作列表不渲染。
- 隔离训练计划与每日修复的动作进度：修复库使用独立的 `repair` 作用域，训练 B 的墙面滑手次数、计时、休息和上限状态不再显示到修复界面；原有训练计划进度键保持兼容。
- 为本次进度隔离更新 Service Worker 缓存至 `train-well-v51`，并同步 `app.js` 版本号，确保已安装的 PWA 获取新逻辑。
- 当当前训练计划的所有动作和组数完成后自动记录今日训练，不再要求再次手动点击“完成今日训练”。
- 为自动结算逻辑更新 Service Worker 缓存至 `train-well-v52`，并同步 `app.js` 版本号。

## 2026-08-14

- 默认语言调整为英文，并将语言切换入口移动到设置页。
- 增加中英文界面文本和动作指导内容。

## 2026-08-10

- 更新训练导航、训练进度记录和恢复活动页面。
- 页面刷新增加网络优先、离线回退逻辑。
